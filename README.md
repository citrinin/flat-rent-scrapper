# Telegram bot for renting an apartment in Lithuania with aruodas.lt

to run bot locally

- change bot API key and query URL in `const.js` file
- install dependencies `npm i`
- start bot `npm start`
